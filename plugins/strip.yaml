name: strip
command:
  linux: strip [-o $destination ] $source
  windows: strip.exe [-o $destination ] $source
element:
  tag: strip
  value:
    name: source
    type: path
    required: true
  attributes:
    - name: destination
      type: path
      required: false